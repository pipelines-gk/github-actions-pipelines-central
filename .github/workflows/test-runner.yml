name: Test Local Runner

on:
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: [self-hosted, gk-cluster]
    steps:
      - name: Confirm runner is working
        run: |
          echo "✅ Runner está funcionando"
          echo "🏷️ Etiquetas esperadas OK"
          hostname
