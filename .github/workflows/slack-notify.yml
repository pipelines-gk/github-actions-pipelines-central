name: Slack Notification Workflow

on:
  workflow_call: # 🚀 Esto hace que el workflow sea reutilizable

jobs:
  notify_slack:
    runs-on: ubuntu-latest
    steps:
      - name: Send Notification to Slack
        uses: slackapi/slack-github-action@v1.24.0
        with:
          payload: |
            {
              "text": "🚀 Workflow *${{ github.workflow }}* terminó en *${{ github.event.workflow_run.conclusion }}* en *${{ github.repository }}*",
              "attachments": [
                {
                  "text": "🔗 Ver detalles en GitHub",
                  "actions": [
                    {
                      "type": "button",
                      "text": "Abrir GitHub",
                      "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                    }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: "https://hooks.slack.com/services/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"
